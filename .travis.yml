matrix:
    include:
        - name: "Linux"
          services:
            - docker
          language: minimal
          env:
            - OS=linux
        - name: "Mac OSX"
          os: osx
          language: shell
          osx_image: xcode10.1
          env: OS=osx
          addons:
              homebrew:
                packages:
                - gcc@8
                - verilator


script:
    - ./scripts/ci.sh

deploy:
  - provider: script
    script: /bin/bash scripts/deploy.sh
    on:
      branch: master
      tags: true
